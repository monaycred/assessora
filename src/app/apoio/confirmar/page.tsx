'use client'; import {Suspense,useState} from 'react'; import {useSearchParams} from 'next/navigation'; import Button from '@/components/ui/Button'; import {HeartHandshake} from 'lucide-react';
function Confirmation(){const params=useSearchParams(),[done,setDone]=useState(false),[error,setError]=useState('');async function confirm(){const r=await fetch('/api/support/confirm',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token:params.get('token')})});const d=await r.json();if(!r.ok)setError(d.error);else setDone(true)}return <main className="grid min-h-screen place-items-center bg-emerald-50 p-4"><section className="max-w-md rounded-3xl bg-white p-8 text-center shadow-xl"><HeartHandshake className="mx-auto h-12 w-12 text-emerald-600"/><h1 className="mt-4 text-2xl font-bold text-slate-950">Contato de apoio</h1>{done?<p className="mt-4 text-emerald-800">Aceite confirmado. Você poderá receber pedidos de apoio dessa pessoa.</p>:<><p className="my-4 text-sm leading-6 text-slate-600">Ao aceitar, a Iasmin poderá avisar quando essa pessoa pedir seu apoio. Nenhum diário ou detalhe da jornada será compartilhado.</p>{error&&<p className="mb-3 text-sm text-red-700">{error}</p>}<Button onClick={confirm}>Aceitar ser contato de apoio</Button></>}</section></main>}
export default function ConfirmSupport(){return <Suspense><Confirmation/></Suspense>}
