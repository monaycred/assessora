'use client';
import {useEffect,useState} from 'react'; import {BookOpen,CalendarDays,ExternalLink,GraduationCap,Lightbulb,Loader2,Search} from 'lucide-react'; import Button from '@/components/ui/Button'; import Modal from '@/components/ui/Modal';
type Item={id:string;item_type:string;title:string;description:string|null;provider:string|null;external_url:string|null;metadata:Record<string,any>};
const tabs=[['all','✨ Para você'],['tip','💡 Dicas'],['movie','🎬 Filmes e séries'],['course','🎓 Cursos grátis'],['event','🏃 Eventos']];
export function DiscoverView(){const[type,setType]=useState('all'),[items,setItems]=useState<Item[]>([]),[loading,setLoading]=useState(true),[search,setSearch]=useState(''),[open,setOpen]=useState(false),[notice,setNotice]=useState('');useEffect(()=>{setLoading(true);fetch(`/api/discovery?type=${type}`).then(r=>r.json()).then(d=>setItems(d.items||[])).finally(()=>setLoading(false))},[type]);const shown=items.filter(i=>(i.title+' '+(i.provider||'')).toLowerCase().includes(search.toLowerCase()));async function suggest(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),r=await fetch('/api/discovery',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(f))}),d=await r.json();setNotice(d.message||d.error);if(r.ok)setTimeout(()=>setOpen(false),1200)}return <div className="space-y-5"><header><h1 className="text-3xl font-black text-slate-950">Descobrir</h1><p className="text-slate-600">Dicas, cursos e atividades para sua rotina.</p></header><label className="flex min-h-12 items-center gap-2 rounded-2xl border-2 border-blue-100 bg-white px-4"><Search className="h-5 w-5 text-blue-700"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Buscar dicas, cursos ou eventos" className="min-w-0 flex-1 outline-none"/></label><div className="flex gap-2 overflow-x-auto pb-1">{tabs.map(([id,label])=><button key={id} onClick={()=>setType(id)} className={`whitespace-nowrap rounded-full px-4 py-3 text-sm font-bold ${type===id?'bg-blue-600 text-white':'bg-blue-50 text-slate-800'}`}>{label}</button>)}</div><section className="overflow-hidden rounded-3xl bg-gradient-to-br from-emerald-200 via-cyan-100 to-purple-200 p-5 shadow"><div className="flex items-center gap-4"><span className="text-6xl">💡📖</span><div><span className="rounded-full bg-white/70 px-3 py-1 text-xs font-bold text-emerald-900">Dica da semana</span><h2 className="mt-2 text-2xl font-black text-slate-950">Como criar hábitos que realmente duram</h2></div></div></section><div className="flex items-center justify-between"><h2 className="text-xl font-black text-slate-950">Conteúdos</h2><Button variant="outline" size="sm" onClick={()=>setOpen(true)}>+ Sugerir conteúdo</Button></div>{loading?<Loader2 className="mx-auto h-6 w-6 animate-spin"/>:<div className="grid gap-4 sm:grid-cols-2">{shown.map(i=><article key={i.id} className={`rounded-3xl border-2 p-5 shadow-sm ${i.item_type==='course'?'border-purple-200 bg-gradient-to-br from-purple-50 to-blue-50':i.item_type==='event'?'border-orange-200 bg-orange-50':'border-emerald-200 bg-emerald-50'}`}><span className="mb-3 inline-flex rounded-full bg-white px-3 py-1 text-xs font-black text-blue-800">{i.item_type==='course'?'GRATUITO':i.item_type==='event'?'EVENTO':'DICA'}</span><div className="flex gap-3"><span className="text-3xl">{i.item_type==='course'?'🎓':i.item_type==='event'?'📅':'💡'}</span><div><h3 className="font-black text-slate-950">{i.title}</h3>{i.provider&&<p className="text-sm font-semibold text-slate-600">{i.provider}</p>}</div></div>{i.description&&<p className="mt-3 text-sm leading-6 text-slate-700">{i.description}</p>}{i.external_url&&<a href={i.external_url} target="_blank" rel="noopener noreferrer" className="mt-4 flex min-h-11 items-center justify-center gap-2 rounded-xl bg-blue-600 font-bold text-white">Ver detalhes<ExternalLink className="h-4 w-4"/></a>}</article>)}</div>}<Modal open={open} onClose={()=>setOpen(false)} title="Sugerir conteúdo"><form onSubmit={suggest} className="space-y-3"><select name="item_type" className="w-full rounded-xl border p-3" required><option value="course">Curso gratuito</option><option value="tip">Dica</option><option value="movie">Filme</option><option value="series">Série</option><option value="event">Evento</option></select><input name="title" className="w-full rounded-xl border p-3" placeholder="Título" required/><input name="provider" className="w-full rounded-xl border p-3" placeholder="Instituição ou responsável"/><input name="external_url" type="url" className="w-full rounded-xl border p-3" placeholder="https://..."/><textarea name="description" className="w-full rounded-xl border p-3" placeholder="Descrição" rows={4}/>{notice&&<p className="text-sm font-bold text-blue-800">{notice}</p>}<Button type="submit" className="w-full">Enviar para análise</Button></form></Modal></div>}
